---
title: "Unweighted Maximum Cut SWAP (*)"
acronym: "UMCS"
book_id: "A.5.4"
categories: ["Local Optimality"]
status: "p-complete"
tags: []
draft: false
confidence: "high"
references: [119, 242, 278, 324, 247, 316]
---

## Given

An undirected graph $G = (V, E)$.

## Problem

Find a locally optimal subset of vertices. A subset is locally optimal if it has maximum cost among its neighbors. The cost is the sum of the number of edges leaving vertices in the subset; each edge has a weight of one. A neighbor of a subset $S$ is a set of size $|S|$ whose symmetric difference with $S$ contains one vertex.

## Status

$\P$-complete (Papadimitriou, Sch¨ affer, and Yannakakis [3]).

## Remarks

The weighted version of the problem in which each edge is given an integer weight encoded in binary is PLS-complete [3][4]. If the edge weights are encoded in unary, the problem is FP-complete. The Different than Majority Labeling Problem is equivalent to this problem [4]. A different than majority labeling of an undirected graph $G = (V, E)$ is a labeling of the vertices with either $0$ or $1$ such that each vertex is labeled differently than at least half of its neighbors. Luby describes an $\NC$ algorithm for a related problem [5]. Sarnath and He show that a lexicographic version of the problem is $\P$-complete [6], see Problem A.5.8.
