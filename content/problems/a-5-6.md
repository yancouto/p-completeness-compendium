---
title: "Unweighted Graph Partitioning SW AP"
acronym: "UGPS"
book_id: "A.5.6"
categories: ["Local Optimality"]
status: "p-hard"
tags: []
draft: false
confidence: "high"
references: [278, 324, 178, 320, 319]
---

## Given

An undirected graph $G = (V, E)$ with $2n$ vertices.

## Problem

Find a locally optimal partition of $V$ into two equal size sets. The partition $V_1$ and $V_2$ is locally optimal if the number of edges going between $V_1$ and $V_2$ a minimum among all neighbors of the partition. A neighbor is a partition that can be obtained from $V_1$ and $V_2$ by swapping one element of $V_1$ with one element of $V_2$.

## Status

$\P$-complete (Papadimitriou, Schäffer, and Yannakakis [1]).

## Remarks

The weighted version of the problem in which the weights are encoded in binary is $\mathsf{PLS}$-complete [1][2]. If the weights are encoded in unary, the problem is $\mathsf{FP}$-complete (Johnson, Papadimitriou, and Yannakakis [3]). Another problem the Fiduccia- Mattheyses Heuristic Graph Partitioning SWAP (FM-Graph Partitioning in [2]) is defined similarly to UGPS except for the neighbors of a solution. The neighborhood of a solution may be obtained by a sequence of up to $|V|$ swaps following the Fiduccia- Mattheyses heuristic (see [2]). FM-Graph Partitioning is also $\mathsf{FP}$- complete [4][2]. In fact, a simpler local search problem in which each partition has just one neighbor, the one obtained after the first step of the Fiduccia-Mattheyses heuristic, is also $\mathsf{FP}$-complete [4][2]. This problem is called FM in [4] and FM-SWAP in [2]. In [4] they also show that a simulated annealing based on a SWAP neighborhood and a “cooling schedule” is $\P$-complete. Several additional results are presented proving other graph partitioning strategies are $\P$-hard. See [5][4] for further details.
