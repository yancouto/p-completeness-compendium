---
title: "Restricted Chromatic Alternating Graph"
acronym: ""
book_id: "A.2.5"
categories: ["Graph Theory"]
status: "p-complete"
tags: []
draft: false
import_confidence: "high"
references: [231]
---

## Given

An alternating graph $G = (V, E)$, two natural numbers $k$ and $m$ (where $k \leq m \leq \log |V|$), a coloring $c : E \to \{1, \ldots, m\}$, and two vertices $s$ and $t$. Note that the coloring is an unrestricted assignment of colors to the edges. It may assign the same color to several edges incident to a common vertex. See Problem A.2.3 for the definition of an alternating graph.

## Problem

Are there $k$ different colors $i_1, \ldots, i_k \in \{1, \ldots, m\}$ such that $\text{apath}(s, t)$ holds in the subgraph of $G$ induced by the edges with colors $i_1, \ldots, i_k$? See Problem A.2.3 for the definition of apath.

## Status

P-complete problem from Greenlaw et al. (1995), Problem A.2.5.

## Remarks

The problem remains P-complete if the vertices are restricted to being breadth-first ordered. When generalized to hierarchical graphs, the problem becomes NP-complete.
